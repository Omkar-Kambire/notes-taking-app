<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Task</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            class="block w-full rounded-md outline-none px-5 py-2 bg-zinc-800"
            type="text"
            placeholder="Enter Title" name="title"
          />
          <textarea
            class="block w-full rounded-md outline-none mt-2 px-5 py-2 bg-zinc-800 resize-none"
            placeholder="Details about the task..." name="details"
          ></textarea>
          <input
            class="mt-3 bg-green-500 p-2 rounded-md active:scale-95 hover:scale-102 hover:bg-green-700"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks p-10 flex gap-4 flex-wrap">
        <% if (files.length > 0) { %> <% files.forEach(function (val) { %>
        <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
          <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
          <a class="text-blue-500 inline-block mt-3" href="/file/<%= val %>">Read More</a>
        </div>
        <%}) %> <%} else {%>
        <h3 class="text-zinc-400">No tasks yet</h3>
        <% }%>
      </div>
    </div>
  </body>
</html>
